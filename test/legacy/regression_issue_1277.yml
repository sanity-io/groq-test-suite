name: 'Regressions: issue 1277'
documents: []
tests:
- name: Issue 1277
  query: |-
    *[_type == "team" && @[""] == "bar"] {
      _id
    }
  result: []
  features:
  - groqV1
- name: Issue 1277
  query: |-
    *[_type == "team" && @[""] == "bar"] {
      _id
    }
  error: true
  features:
  - groqV2
